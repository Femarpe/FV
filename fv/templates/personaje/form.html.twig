{% extends 'base.html.twig' %}

{% block title %}Crear personaje{% endblock %}

{% block body %}
    <h1>📝 Crear / Editar personaje</h1>

    {{ form_start(formulario) }}
        {{ form_widget(formulario) }}
        <button class="btn btn-primary">Guardar</button>
    {{ form_end(formulario) }}
{% endblock %}
